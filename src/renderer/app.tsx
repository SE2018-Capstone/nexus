import * as React from 'react';

export class App extends React.Component<{}, {}> {
  render() {
    return (
      <div style={{width: '100%', height: '100%'}}>
        <h2 style={{color: 'cyan'}}>Welcome to React with Typescript!</h2>
      </div>
    );
  }
}
